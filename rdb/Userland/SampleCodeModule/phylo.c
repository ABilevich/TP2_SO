#include <phylo.h>

void phylo(s_phylo *me)
{
    while (1)
    {
        // me->state = THINKING;
        // wait(1); //rand
        // me->state = HUNGRY;
        // //ver que palito es el de menor numero
        // int small = me->left_chop->id;
        // int big = me->right_chop->id;
        // semWait(small);
        // semWait(big);
        // me->state = EATING;
        // semPost(small);
        // semPost(big);
    }
}